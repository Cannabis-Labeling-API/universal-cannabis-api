required:
  - manifestItems
properties:
  regulatorEvent:
    $ref: ./Regulator.yaml

  manifestItems:
    type: array
    example:
      - "qr.example.com"
      - "qr.test.com"
    items:
      type: string

type: object
