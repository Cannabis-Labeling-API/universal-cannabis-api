type: object
required:
  - identifier
  - name
  - collectionType
properties:
  id:
    $ref: "Id.yaml"
  createdDate:
    type: string
    format: date-time
  identifier:
    type: string
    description: Metrc Id / Batch Id / Case Id
  name:
    type: string
    description: Name for your collection
  collectionType:
    $ref: "CollectionType.yaml"
  eaches:
    $ref: "Id.yaml"
