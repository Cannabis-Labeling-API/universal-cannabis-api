type: object
required:
  - id
  - eaches
  - regulatorEvents
  - batchId
  - productId

properties:
  id:
    $ref: Id.yaml
  eaches:
    $ref: IdArray.yaml
  regulatorEvents:
    description: Sorted list (oldest first) of regulator events
    type: array
    items:
      $ref: Regulator.yaml
